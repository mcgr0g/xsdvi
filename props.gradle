ext{
    buildSubLocation = "libs"
    examplesDir = "examples"
    xsdDir = "".concat(sourceSets.main.output.getResourcesDir().toString())
            .concat('\\')
            .concat(examplesDir)
            .concat("\\")
            .concat("xsd")
    exampleFile = "faktura"
    targetFileExt = "svg"

}

task showExtProps{
    group = 'user-task'
    description = 'show all external properties'
    println "exampleFile = $xsdDir"
            .concat("\\")
            .concat(exampleFile)
            .concat(".xsd")
}