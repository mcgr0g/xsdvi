// Set properties
ext {
    jarLibs = "$buildDir/libs".replaceAll('\\\\','/')
    fatName = "$project.name-uber"
    tstDir = "$buildDir/tests".replaceAll('\\\\','/')
    examplesDir = "examples"
    xsdDir = "".concat(sourceSets.main.output.getResourcesDir().toString())
            .concat('\\')
            .concat(examplesDir)
            .concat("\\")
            .concat("xsd")
    exampleFile = "faktura"
    targetFileExt = "svg"
}